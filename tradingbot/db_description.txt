CREATE TABLE  Transactions(
id,
start_time,
symbol,
ask,
quantity,
end_time,
bid,
receipts,
result);

CREATE TABLE  Orders(
id REAL,
start_time REAL,
symbol text,
purchased_quantity REAL(1,9),
purchase_price REAL,
sold_quantity REAL,
profit REAL,
end_time REAL,
result);


CREATE TABLE  tmp(id, name);